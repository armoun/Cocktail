<manifest
    package="com.magie_pooh.mylibrary"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name">
        <activity
            android:name=".debuger.ui.activity.RecipeActivity"
            android:configChanges="orientation|screenSize"
            android:launchMode="singleTask"
            android:theme="@style/AppTranslucent"/>
        <activity
            android:name=".debuger.ui.activity.RecipePrefActivity"
            android:theme="@style/AppTranslucent"/>

        <service android:name=".debuger.service.ShakeListenerService"/>
    </application>
</manifest>
